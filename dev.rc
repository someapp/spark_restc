export ERLANG_HOME=/opt/erlang
export ERL_LIBS=${ERLANG_HOME}/lib
export PATH=${ERLANG_HOME}/bin:$PATH
export ERLANG_COOKIE=~/.erlang.cookie

echo ${ERLANG_HOME}
echo ${ERL_LIBS}
echo ${PATH}
echo `cat ${ERLANG_COOKIE}`
